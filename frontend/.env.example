# 后端 API 地址配置
# ==================
#
# 本地开发（默认）:
#   VITE_API_BASE=/api
#   - Vite 代理会自动把 /api 转发到 http://127.0.0.1:8000
#   - 需要先启动后端: cd ../backend && uvicorn main:app --reload
#
# Vercel 部署（推荐方式一：使用 vercel.json rewrites）:
#   不设置此变量，保持默认值 /api
#   在 vercel.json 中配置 rewrites 代理到 Railway 后端
#
# Vercel 部署（推荐方式二：直接指定后端地址）:
#   VITE_API_BASE=https://your-project.up.railway.app
#   - 直接请求 Railway 后端，无需 vercel.json 代理
#
VITE_API_BASE=/api
